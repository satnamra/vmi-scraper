# VMI Scraper

This project is designed to scrape data from the VMI website.

## Setup

1. Clone the repository.
2. Install dependencies.
3. Configure environment variables (e.g., in a `.env` file).

## Usage

Run the scraper:

```bash
python src/main.py
```